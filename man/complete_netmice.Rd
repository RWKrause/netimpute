% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/netmice.R
\name{complete_netmice}
\alias{complete_netmice}
\title{Extract one completed data.frame/network-list from a netmids object}
\usage{
complete_netmice(x, action = 1)
}
\arguments{
\item{x}{A `netmids` object from \code{\link{netmice}}.}

\item{action}{Which imputation to extract (integer, 1..m).}
}
\value{
A list with `data` (completed data.frame) and `networks`
  (completed list of adjacency matrices).
}
\description{
Extract one completed data.frame/network-list from a netmids object
}
\examples{
set.seed(1)
n <- 25
friends <- matrix(rbinom(n * n, 1, 0.15), n, n)
diag(friends) <- 0
friends[sample(which(row(friends) != col(friends)), 30)] <- NA
attrs <- data.frame(age = rnorm(n, 35, 8),
                    gender = sample(c("F", "M"), n, TRUE))
attrs$age[sample(n, 4)] <- NA

\donttest{
fit <- netmice(attrs, list(friends = friends), m = 2, maxit = 2,
               seed = 1, printFlag = FALSE)
completed <- complete_netmice(fit, 1)
str(completed$data)
anyNA(completed$net_list$friends)
}
}
\seealso{
\code{\link{netmice}} to create the object;
  \code{\link{plot.netmids}} and \code{\link{print.netmids}} to inspect it.
}
